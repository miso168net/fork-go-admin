# CDP:9229 + fork260506-go-admin-ui 排查紀錄

> **日期**:2026-05-09
> **語言**:zh-TW
> **撰寫**:Claude Code(對話式產出)
> **觸發場景**:`docker compose --profile sqlite up -d` 起來後,瀏覽器打 `http://localhost:8080/#/login` 看到 webpack-dev-server overlay 報 `r.component is not a function`(Vue 3 render error)
> **產出脈絡**:本檔是事後的 lessons-learned,純粹紀錄 CDP 操作技法 + 排查 reasoning,不影響任何已 merge 的 spec / plan

---

## 0. TL;DR(60 秒重點)

| 項 | 內容 |
|---|---|
| **症狀** | login 頁面在瀏覽器顯示「Uncaught runtime errors: r.component is not a function」overlay |
| **根因** | fork260506-go-admin-ui 的 Vue 3 migration 沒收尾:`main.js:40` 已把 `vue-particles` plugin 全域註冊註解掉,但 `views/login/index.vue` 還 `import Particles from '@tsparticles/vue3'` 又把它當 local component 註冊。Plugin object `{ install: P }` 被 Vue 當 component render 時,Vue 把 `install` 當 render 函式呼叫 → `r.component(...)` 噴 |
| **修法** | 註解 `login/index.vue` 三處(template `<vue-particles>` block / `import Particles` / `components: { VueParticles: Particles }`)|
| **HMR 卡關** | 改完檔 vue-cli 確實 rebuild 了,但 webpack-dev-server 的 in-memory bundle 對 broken module state 沒復原。Page.reload(ignoreCache=true)沒救;**`docker compose restart go-admin-ui` 清掉 dev-server 整個記憶體後復活** |
| **CDP 用途** | 瀏覽器在 host(WSL2 外的 Windows Edge),Claude 在 WSL2 內 → 跨層用 CDP `:9229` 才能直接讀頁面 DOM、reload、抓 webpack overlay 文字。比讓使用者口述 / 截圖快 |

---

## 1. CDP:9229 環境

### 1.1 端點型態

CDP 在 Edge / Chrome 啟動時加 `--remote-debugging-port=9229` 暴露,有兩種 surface:

| Surface | 用途 | 工具 |
|---|---|---|
| **HTTP REST**(`http://127.0.0.1:9229/json*`) | 列頁面、取版本、新開 tab、關 tab | `curl` |
| **WebSocket**(`ws://127.0.0.1:9229/devtools/page/<id>`) | Runtime / Page / DOM / Network domain 雙向 RPC | 任意 WS client |

### 1.2 HTTP REST 一覽

```bash
# 探活 + 取 browser-level WS URL
curl -s http://127.0.0.1:9229/json/version | jq .

# 列出所有 page / iframe / service-worker
curl -s http://127.0.0.1:9229/json | jq .
# 或不裝 jq:
curl -s http://127.0.0.1:9229/json | python3 -m json.tool

# 篩出 type=page 且 title 含 'go-admin' 的 WS URL
curl -s http://127.0.0.1:9229/json | python3 -c '
import sys, json
ps = json.load(sys.stdin)
print([p["webSocketDebuggerUrl"]
       for p in ps
       if p.get("type") == "page" and "go-admin" in p.get("title", "")][0])
'
```

### 1.3 為何要 WebSocket(只用 HTTP 不夠)

HTTP REST **沒有** Runtime.evaluate / Page.reload / DOM 操作。要在頁面 context 跑 JS、操控頁面、讀 DOM,只能走 WS。

---

## 2. WS Tooling — 嘗試與最終選擇

### 2.1 嘗試過的選項

| 工具 | 結果 |
|---|---|
| `pip install websocket-client` | ❌ 沒 pip,系統 Python |
| `python3 -m pip install ...` | ❌ `No module named pip` |
| `wscat`(/usr/bin/wscat) | ⚠ 互動模式為主,scripted 多訊息 RPC 不順手 |
| **`node` + `ws` 套件**(在 `/tmp/cdp-tmp/` 安裝) | ✅ 採用 |

### 2.2 最終配置

```bash
mkdir -p /tmp/cdp-tmp && cd /tmp/cdp-tmp && npm install --no-audit --no-fund ws
# → ws 套件落在 /tmp/cdp-tmp/node_modules/ws
```

之後跑時用 `NODE_PATH=/tmp/cdp-tmp/node_modules node -e '...'`,**inline 寫整段 CDP 邏輯**。

### 2.3 為何用 inline `node -e`,不用 `node probe.js`

實作中發現:harness 對「執行未顯示在 transcript 的 .js 檔」會擋(防止無記錄的 code execution)。**用 inline `-e` 把整段 JS 嵌進 Bash 命令裡,可被 transcript 完整看見,不會被擋**。代價是 Bash 跳脫得寫好(雙引號、JSON 跳脫、regex 反斜線連串)。

---

## 3. CDP RPC 通用骨架

```bash
export PAGE_WS=$(curl -s http://127.0.0.1:9229/json | python3 -c "
import sys, json
ps = json.load(sys.stdin)
print([p['webSocketDebuggerUrl']
       for p in ps
       if p.get('type')=='page' and 'go-admin' in p.get('title','')][0])")

cd /tmp/cdp-tmp && NODE_PATH=/tmp/cdp-tmp/node_modules node -e '
const WS = require("ws");
const ws = new WS(process.env.PAGE_WS);
let nid = 1; const pending = new Map();

const send = (m, p={}) => new Promise(r => {
  const id = nid++; pending.set(id, r);
  ws.send(JSON.stringify({ id, method: m, params: p }));
});

const ev = async expr => (await send("Runtime.evaluate", {
  expression: expr, returnByValue: true
})).result?.result;

ws.on("open", async () => {
  await send("Runtime.enable");
  // ▼ 你的 CDP 互動邏輯
  console.log("title:", (await ev("document.title"))?.value);
  ws.close();
});
ws.on("message", raw => {
  const m = JSON.parse(raw);
  if (m.id && pending.has(m.id)) { pending.get(m.id)(m); pending.delete(m.id); }
});
ws.on("error", e => { console.error("WSerr", e.message); process.exit(1); });
ws.on("close", () => process.exit(0));
'
```

要點:
- `pending: Map<id, resolveFn>` 把每個 RPC 的 promise 對到回應
- `Runtime.evaluate` 一定要設 `returnByValue: true`,否則回的是 RemoteObject(只能拿到 type/objectId,要再用 Runtime.getProperties 拆)
- 程式 await 完最後一個動作後 `ws.close()`,在 `close` event 裡 `process.exit(0)`

---

## 4. 排查重要 CDP 配方

### 4.1 探 DOM 渲染狀態

```js
const checks = {
  "html len":           "document.documentElement.outerHTML.length",
  "body len":           "document.body.innerHTML.length",
  "#app exists":        "!!document.getElementById('app')",
  "#app innerHTML len": "document.getElementById('app')?.innerHTML.length || 0",
  "all elements":       "document.querySelectorAll('*').length",
  "form count":         "document.querySelectorAll('form').length",
  "input count":        "document.querySelectorAll('input').length",
  ".login-form?":       "!!document.querySelector('.login-form')",
};
for (const [k, e] of Object.entries(checks)) {
  console.log(k.padEnd(22), ":", JSON.stringify((await ev(e))?.value));
}
```

> 這次排查的關鍵發現:`#app innerHTML len = 20` — Vue 有 mount,但 root component (login.vue) 在第一個子元件就 throw,所以 `#app` 內幾乎是空的。

### 4.2 強制硬刷頁面(繞過快取)

```js
await send("Page.enable");
await send("Page.reload", { ignoreCache: true });
```

### 4.3 抓 webpack-dev-server overlay 的訊息

webpack-dev-server v4+ 把 overlay 包在一個**iframe**(`#webpack-dev-server-client-overlay`)裡,iframe 是 `about:blank` 但 same-origin,可直接讀 contentDocument:

```js
const overlayText = await ev(`(()=>{
  const host = document.querySelector("#webpack-dev-server-client-overlay");
  if (!host) return "(no overlay)";
  const ifr = host.tagName==="IFRAME" ? host : host.querySelector("iframe");
  if (!ifr) return "(no iframe)";
  try { return ifr.contentDocument?.body?.innerText?.substring(0, 4000); }
  catch (e) { return "ERR " + e.message; }
})()`);
```

> ⚠ 如果用了 shadow DOM(舊版 webpack-dev-server)需要走 `host.shadowRoot.innerHTML`;新版改用 iframe。先 dump `host.outerHTML` 確認哪一種。

### 4.4 Bash 跳脫陷阱

inline `node -e '...'` 內若 JS 還要寫 regex / 反斜線,Bash 會吃一輪、JS 字串再吃一輪 → **正則的 `\s` 要寫成 `\\\\s`**:

```bash
node -e '
  const ev = ...
  const m = await ev("text.match(/Uncaught[\\\\s\\\\S]{0,400}/)");
'
```

(Bash 把 `\\\\s` → `\\s`,JS 字串再把 `\\s` → `\s`,regex 才看到 `\s`。)

---

## 5. fork260506-go-admin-ui 的本次 bug 完整解剖

### 5.1 錯誤訊息

```
Uncaught runtime errors:
ERROR
r.component is not a function
TypeError: r.component is not a function
    at P (http://127.0.0.1:8080/js/chunk-vendors.js:1372:5)
    at renderComponentRoot (.../chunk-vendors.js:8434:30)
    at ReactiveEffect.componentUpdateFn [as fn] (.../chunk-vendors.js:9964:46)
    at mountComponent (.../chunk-vendors.js:9871:7)
    at processComponent (.../chunk-vendors.js:9823:9)
    at patch (.../chunk-vendors.js:9334:11)
    at mountChildren (.../chunk-vendors.js:9585:7)
    at mountElement (.../chunk-vendors.js:9501:7)
```

### 5.2 stack trace 解讀(自下而上)

| 位置 | 意義 |
|---|---|
| `mountElement → mountChildren → patch → processComponent → mountComponent` | Vue 3 在 mount 一個 child component |
| `setupRenderEffect → componentUpdateFn → renderComponentRoot` | 該 component 進到 render 階段 |
| `P` | 該 component 的「render 函式」是 `P` |
| `r.component is not a function` | `P` 內第一句是 `r.component("vue-particles", w)`;`r` 是 Vue 在 render 階段塞給它的東西,**不是** Vue app instance |

### 5.3 拿 dev bundle 對源碼

vue-cli dev mode 不 minify,可直接 curl 抓:

```bash
curl -s http://127.0.0.1:8080/js/chunk-vendors.js | sed -n '1368,1380p'
```

得到:

```js
P = (r, e) => {
  r.component("vue-particles", w),
  (async () => (
    _tsparticles_engine__WEBPACK_IMPORTED_MODULE_1__.tsParticles.init(),
    e.init && await e.init(_tsparticles_engine__WEBPACK_IMPORTED_MODULE_1__.tsParticles),
    dispatchEvent(new CustomEvent("particlesInit", { detail: ... }))
  ))();
};
```

→ `P` 是 `@tsparticles/vue3` 的 **plugin install function**(`install(app, options)`),不是 component 的 render。

### 5.4 為什麼 Vue 把 install 當 render 呼叫

`@tsparticles/vue3` 的預設輸出大致是:

```js
{ install: (app, options) => { app.component("vue-particles", VueParticlesComp); ... } }
```

**正確用法**(plugin):

```js
// main.js
import { createApp } from 'vue'
import Particles from '@tsparticles/vue3'
const app = createApp(App)
app.use(Particles, { ... })
```

**錯誤用法**(把 plugin object 當 component):

```vue
<script>
import Particles from '@tsparticles/vue3'
export default {
  components: { VueParticles: Particles }   // ← Particles 是 plugin,不是 component
}
</script>
<template>
  <vue-particles ... />
</template>
```

Vue 3 拿到 `{ install: P }` 當 component definition,因為沒有 `render`/`setup`/`template`,fallback 路徑下 Vue 會把第一個 callable 屬性當 render → `install` 被當 render 呼叫 → 第一個參數本來該是 app instance,卻是 Vue 內部塞進來的 ctx → 沒 `.component()` → 噴。

### 5.5 fork 為什麼會有這 bug

`fork260506-go-admin-ui/src/main.js:40` 已經有註釋:

```js
// import VueParticles from 'vue-particles'
// Note: vue-particles 不支持 Vue 3,需要后续处理
```

→ 維護者意識到 vue-particles(Vue 2 版)不支援,把全域 `app.use()` 拿掉了。但 `views/login/index.vue` 還沒同步處理,改成 import `@tsparticles/vue3`(Vue 3 版)當**component**塞進 `components` object。理應改用 `app.use(Particles)` 全域註冊,並把 template 的 `<vue-particles>` 留著(plugin install 時 `app.component("vue-particles", ...)` 註冊它)。

### 5.6 本次採用的最小修法

註解 `views/login/index.vue` 三處:

| 行 | 改前 | 改後 |
|---|---|---|
| ~3-9 (template) | `<div id="particles-js"><vue-particles ... /></div>` | 整塊用 `<!-- ... -->` 註解 |
| 174 (import) | `import Particles from '@tsparticles/vue3'` | `// import Particles ...` |
| 180 (components) | `VueParticles: Particles` | `// VueParticles: Particles` |

**還原指令**(若要保留 fork 原狀):

```bash
git -C fork260506-go-admin-ui checkout src/views/login/index.vue
docker compose --profile sqlite restart go-admin-ui   # 清 dev-server in-memory state
```

### 5.7 長期解(留作後續 PR,不在本次)

修 `main.js`,把 `app.use(Particles, { ... })` 加回來:

```js
import Particles from '@tsparticles/vue3'  // 移到 main.js
// ...
const app = createApp(App)
app.use(Particles)  // 全域註冊 → <vue-particles> 自動可用
```

`login/index.vue` 改成只在 template 用 `<vue-particles>`,不要 local 註冊。

---

## 6. HMR 卡關的處理

### 6.1 表象

- 改了檔 → 容器內 bind-mount 立即看到新內容(`docker exec go-admin-ui grep ... /app/src/views/login/index.vue` 確認過)
- vue-cli logs 出現 `Build finished at HH:MM:SS`(看似 rebuild 完了)
- `Page.reload({ ignoreCache: true })` 後依然同樣 error

### 6.2 真實原因

webpack-dev-server 的 in-memory bundle 在 **broken module state** 下會卡住:HMR 收到 patch 但 vue-cli 的 module graph 內某些 node 維持舊狀態,即使硬刷瀏覽器,server 還是吐舊 chunk。

### 6.3 修法(屢試不爽)

```bash
docker compose --profile sqlite restart go-admin-ui
```

**不是** rebuild image、**不是** down+up(那會慢很多)。`restart` 把 process 從零起一次,記憶體被清空,vue-cli 從 disk 重建 module graph,bundle 乾淨。

> 之後在容器內看到 `Build finished at <new-time>` → reload 瀏覽器 → 修好的版本生效。

### 6.4 何時用 restart vs down/up

| 情境 | 動作 |
|---|---|
| 改了檔但 HMR 沒生效 | `restart`(20–30s) |
| compose 設定改了(env、ports、volumes) | `up -d`(reapply) |
| 想完全清掉 npm install / go module cache | `down -v` 後 `up -d`(慢:首啟 5–15 min) |
| 容器掉到 unhealthy / panic | `restart`(優先);還不行 `down` 看 logs |

---

## 7. 操作流程的 reflection — 下一次怎麼更快

| 教訓 | 改變 |
|---|---|
| 一開始花時間試 pip / wscat 找 WS client | 直接默認用 `node + ws`(已驗證可在 npm 全域 install + inline `-e` pattern) |
| 只看 webpack overlay 的字串就猜「是 vue-particles」 | 對。但接下來應該**立即**驗 `chunk-vendors.js:1372` 抓真正的 `P` 程式碼,再回到 src grep — 這次是先 grep src 再驗 bundle,順序倒了不影響結果但慢了一輪 |
| 改完檔以為 HMR 會吃 → 檢查瀏覽器發現沒效果 → 又改檔 | 第一輪 reload 後若沒效果,**第二步就 restart 容器**;不要無限期等 HMR 自己醒 |
| harness 擋執行 `node probe.js` | 預設用 inline `node -e '...'`,腳本邏輯 transcript 看得見 |
| WSL2 與 Windows 瀏覽器跨層 | CDP `:9229` 是 right tool;比讓使用者來回口述快很多 |

---

## 8. 一頁速查表

```bash
# (1) 探活
curl -s http://127.0.0.1:9229/json/version | python3 -m json.tool

# (2) 取目標頁 WS URL(以 title 包含字串為條件)
export PAGE_WS=$(curl -s http://127.0.0.1:9229/json | python3 -c "
import sys, json
ps = json.load(sys.stdin)
print([p['webSocketDebuggerUrl']
       for p in ps
       if p.get('type')=='page' and 'go-admin' in p.get('title','')][0])")

# (3) 一發 Runtime.evaluate(印 document.title)
cd /tmp/cdp-tmp && NODE_PATH=/tmp/cdp-tmp/node_modules node -e '
const WS = require("ws");
const ws = new WS(process.env.PAGE_WS);
let nid=1; const p=new Map();
const s=(m,pp={})=>new Promise(r=>{const i=nid++;p.set(i,r);ws.send(JSON.stringify({id:i,method:m,params:pp}))});
ws.on("open", async ()=>{
  await s("Runtime.enable");
  const r = await s("Runtime.evaluate",{expression:"document.title",returnByValue:true});
  console.log(r.result.result.value);
  ws.close();
});
ws.on("message", raw=>{const m=JSON.parse(raw);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id);}});
ws.on("close",()=>process.exit(0));
'

# (4) 強刷頁面
# 把上面的 ev() 換成兩條:
# await s("Page.enable")
# await s("Page.reload", { ignoreCache: true })

# (5) HMR 卡住時的逃生鍵
docker compose --profile sqlite restart go-admin-ui
```

---

## 9. 修訂紀錄

| 日期 | 修改 | 作者 |
|---|---|---|
| 2026-05-09 | 初版(對話排查當下產出) | Claude Code |
| 2026-05-09 | 補 §10 第二輪排查(vue-router 3→4 連環 / HMR ws / element-plus overlay) | Claude Code |
| 2026-05-09 | 補 §11 第三輪排查(外部資源稽核 / sidebar 白底白字 / Element Plus 2.x props 棄用 / scss :export 失效) | Claude Code |

---

## 10. 第二輪排查:登入卡關 + HMR ws + 紅色遮罩(2026-05-09 後段)

`fork260506-go-admin-ui` 的 Vue 2→3 migration 漏洞分三批漸次浮現。第一批(§5)只是 login 頁不渲染,第二批是「**login 200 OK 但卡死在 login**」的玄學現象,第三批是「**dashboard 有 render 但被紅色遮罩蓋住**」。每一批都需要新的 CDP 技法。

### 10.0 TL;DR

| 症狀 | 根因 | 修法(已記入 `fork260506-go-admin-ui.md`) |
|---|---|---|
| 點登入後 hash 一直停在 `#/login`,button「登 录 中...」轉個不停 | `permission.js:43` 用 vue-router 3 的 `router.addRoutes()`(已被 vue-router 4 移除)→ TypeError 被 catch 吞 → `next('/login?redirect=/dashboard')` | entry 002:`accessRoutes.forEach(r => router.addRoute(r))` |
| 上一個修了,還是同樣症狀 | `store/modules/permission.js:150` 推 `path: '*'` catch-all(vue-router 4 移除)→ forEach 中 throw → 同樣被 catch 吞 → 同樣 redirect 回 login | entry 003:`path: '/:pathMatch(.*)*'` |
| Console 一直噴 `ws://172.19.0.2:8080/ws ERR_CONNECTION_TIMED_OUT` | webpack-dev-server 4 預設把 publicHost 設成自己 bind 的 IP(container 內 IP),host browser 看不到 docker bridge | entry 004:`vue.config.js` `client.webSocketURL: 'auto://0.0.0.0:0/ws'` |
| Dashboard 渲染 OK,但全頁紅色 webpack overlay 蓋住 | element-plus 2.13.7 / 2.14.0 `el-menu` `calcMenuItemWidth` 把 text node 傳給 `getComputedStyle` → throw runtime error → webpack overlay 蓋頁(配置 `errors:true`)| entry 005:`vue.config.js` `client.overlay.runtimeErrors: false` |

第二批 + 第三批的觀察都仰賴 CDP — 純看 user-facing 畫面看不出原因(login 200 + cookie 寫入 + dashboard 內容 render 都「成功」,但 vue-router 報 `NavigationFailure 16`)。

### 10.1 新 CDP 技法總覽

#### 10.1.1 Router instrumentation pattern(hook push / beforeEach / afterEach / onError)

當 navigation 失敗但**沒拋例外、沒 console error**(像 NavigationFailure 16 是 promise resolve 而非 reject),要看 vue-router 內部過程,只能 monkey-patch:

```js
window.__rlog = [];
const t0 = Date.now();
const ts = () => ((Date.now()-t0)/1000).toFixed(2)+"s";
const router = document.querySelector("#app").__vue_app__.config.globalProperties.$router;

const _push = router.push.bind(router);
router.push = function(...args) {
  window.__rlog.push([ts(), "push", JSON.stringify(args[0])]);
  const p = _push(...args);
  p.then(r => window.__rlog.push([ts(), "push.resolve",
    r ? "FAIL:"+r.type+":"+(r.message||"").slice(0,120) : "OK"]));
  return p;
};
router.beforeEach((to, from) => {
  window.__rlog.push([ts(), "beforeEach", from.fullPath + " -> " + to.fullPath]);
});
router.afterEach((to, from, failure) => {
  window.__rlog.push([ts(), "afterEach", from.fullPath + " -> " + to.fullPath +
    (failure ? " FAIL:"+failure.type+" msg="+(failure.message||"").slice(0,80) : " OK")]);
});
router.onError((err) => { window.__rlog.push([ts(), "onError", String(err).slice(0,200)]); });
```

CDP 流程:
1. Page.reload → wait → 注入 instrumentation
2. 觸發動作(click login button)
3. wait → `Runtime.evaluate` 讀回 `JSON.stringify(window.__rlog)`

關鍵發現:本次 rlog 顯示 `push("/dashboard")` → 30ms 後 `afterEach: /login → /login FAIL:16`,**完全沒進 beforeEach**。意味著 vue-router 在 navigation lifecycle **之前**就 reject 了 — 這是 NavigationFailureDuplicated 的 fast-path:`isSameRouteLocation(from, target)` 短路。

#### 10.1.2 Vue Router 4 NavigationFailureType 對照

| type | 名 | 觸發條件 |
|---|---|---|
| 4 | aborted | guard 回 `false` 或 `next(false)` |
| 8 | cancelled | 中途被另一個 navigation 取代 |
| **16** | duplicated | target.fullPath === currentRoute.fullPath(不進 beforeEach) |

afterEach 收到 `failure` object 時可印 `failure.type` 跟 `failure.message`。`message` 含 `to.fullPath`(比 type 更可讀:`Avoided redundant navigation to current location: "/login?redirect=/dashboard"`)。

> ⚠ 陷阱:NavigationFailureDuplicated 的 from/to 在 afterEach 都顯示**相同的 fullPath**(都是 from),不是 push 進去的 target。要看真正 push 的目標只能從 `_push` hook 拿 args。

#### 10.1.3 Network domain event capture

把 login API 鏈、HMR ws、抓圖請求的時序印出來,單一 page WS 多 method 訂閱:

```js
ws.on("message", m => {
  const msg = JSON.parse(m);
  if (msg.id && pending.has(msg.id)) { pending.get(msg.id)(msg); pending.delete(msg.id); return; }
  // event push (no id)
  if (msg.method === "Network.requestWillBeSent") {
    const u = msg.params.request.url;
    if (u.includes("/api/v1/")) events.push([ts(), "REQ", u]);
  }
  if (msg.method === "Network.responseReceived") {
    const u = msg.params.response.url;
    if (u.includes("/api/v1/")) events.push([ts(), "RES", msg.params.response.status, u]);
  }
  if (msg.method === "Network.webSocketCreated") events.push([ts(), "wsCreate", msg.params.url]);
  if (msg.method === "Network.webSocketHandshakeResponseReceived")
    events.push([ts(), "wsHandshake", msg.params.response.status, msg.params.response.statusText]);
  if (msg.method === "Network.webSocketClosed") events.push([ts(), "wsClose"]);
  if (msg.method === "Runtime.consoleAPICalled") {
    const args = msg.params.args.map(a => a.value !== undefined ? a.value :
      (a.preview ? JSON.stringify(a.preview).slice(0,200) : (a.description||"").slice(0,200))).join(" | ");
    if (args.includes("permission.js DBG")) events.push([msg.params.type, args.slice(0,400)]);
  }
});
```

呼叫前要 `await send("Network.enable")`。

驗證 entry 004 修好之後:
```
created: ws://127.0.0.1:8080/ws        ← 之前是 ws://172.19.0.2:8080/ws
handshake: 101 Switching Protocols     ← 之前 ERR_CONNECTION_TIMED_OUT
```

#### 10.1.4 Vue 3 form 填寫 — 用 native value setter + dispatch event

`v-model` 監聽 native `input`/`change` event。直接設 `el.value = "x"` 不會觸發 reactive — 必須繞過 element 的 setter 用 prototype 上的 native setter:

```js
const setVal = (el, v) => {
  const setter = Object.getOwnPropertyDescriptor(Object.getPrototypeOf(el), "value").set;
  setter.call(el, v);
  el.dispatchEvent(new Event("input", { bubbles: true }));
  el.dispatchEvent(new Event("change", { bubbles: true }));
};
const inputs = document.querySelectorAll(".login-form input");
setVal(inputs[0], "admin");
setVal(inputs[1], "123456");
setVal(inputs[2], "0");
await new Promise(r => setTimeout(r, 200));   // 等 reactive flush
document.querySelector(".login-form .el-button--primary").click();
```

> 為何 `Object.getPrototypeOf(el).value.set`?React/Vue 把 `el.value` 攔截了一個 controlled-component setter,直接設只改 view 不改內部 state。Native setter 在 `HTMLInputElement.prototype` 上,繞過攔截才會觸發 `input` event。

#### 10.1.5 Bundle 檢查 + webpack persistent cache 處理

當你**確定改了 source、container 也 restart 了,還跑舊代碼** — 99% 是 webpack persistent cache 留下舊 module:

```bash
# 看 dev bundle 是不是新 code
curl -s http://127.0.0.1:8080/js/app.js | grep -c "你剛加的字串"

# webpack persistent cache 在這
docker exec go-admin-ui ls /app/node_modules/.cache/    # babel-loader, eslint, etc.

# 清掉 + restart
docker exec go-admin-ui rm -rf /app/node_modules/.cache
docker compose --profile sqlite restart go-admin-ui
```

`docker compose restart` 只重啟 process,**不會清 named volume `node-modules-go-admin-ui` 內的 cache**。所以即使 `Build finished at <newtime>` 出現,webpack 看 cache 認為「沒變」,bundle 還是舊的。

實測 symptom:bundle 同時有新舊兩個版本(`MODULE_7` 是新 code、`MODULE_6` 是舊 code 殘留),browser 載到的 entry 用舊的。

#### 10.1.6 Source-level `console.log` 注入 + CDP 收集

當你想知道 vue-router beforeEach 內部某個變數的值,而 instrumentation 又不夠細(beforeEach 在 navigation lifecycle 之前就被短路),最直接:**在 source 加 console.log,等 rebuild,用 CDP `Runtime.consoleAPICalled` event 收**:

```diff
   const hasToken = getToken()
+  console.log('[permission.js DBG]', { from: from.fullPath, to: to.fullPath,
+    hasToken: hasToken ? 'YES('+hasToken.slice(0,20)+')' : 'NO',
+    rawCookie: document.cookie.slice(0, 200) })
```

確認的事:
- `[permission.js DBG]` 收到 `hasToken: YES(...)` → 排除「cookie 沒讀到」
- 加第二段在 forEach 後 → 收到 `[permission.js DBG] CAUGHT Error: Catch all routes ("*") must now be defined...` → root cause(entry 003)立刻浮現

修完後**記得清掉 debug print**(別忘了 reconciliation 也會偵測 — Detection grep 是 `runtimeErrors:false` 而不是 `permission.js DBG`,debug print 留著 detection 仍 [x] 但 source 髒)。

### 10.2 二輪 bug chain 完整解剖

#### 10.2.1 `addRoutes` → `addRoute`(entry 002)

vue-router 3:`router.addRoutes(arr)` 一次加多個。
vue-router 4:**移除**該 API,改 `router.addRoute(route)` 單個加;多個就 forEach。

`fork260506-go-admin-ui` 在 `package.json` 已經升 `vue-router@^4.4.0`,但 `permission.js` 的 navigation guard 還在用 vue-router 3 的 `addRoutes(accessRoutes)`,call 到 undefined → `TypeError`。

**陰險**:這個 throw 在 `try {}` 裡,catch 直接 `next(\`/login?redirect=${to.path}\`)` 把 user redirect 回 login。沒 console error、沒 unhandledrejection、沒 onError — 全靜默。要看到這個 throw 必須加 `console.log` 在 catch。

#### 10.2.2 `path: '*'` → `path: '/:pathMatch(.*)*'`(entry 003)

vue-router 3:catch-all 寫 `path: '*'`。
vue-router 4:**棄用**,改用命名 param + 自定義 regexp:`path: '/:pathMatch(.*)*'`(greedy 0+)或 `'/:pathMatch(.*)'`(單段)。

`router.addRoute({ path: '*', ... })` 在 vue-router 4 同步 throw:
```
Error: Catch all routes ("*") must now be defined using a param with a custom regexp.
```

**陰險 +1**:這個 throw 出現在 entry 002 修完之後 — `forEach(addRoute)` 一路加到最後一個 catch-all 才炸,前面 22 個動態路由都加進去了(`router.getRoutes().length` 從 12 → 34 看起來像「修好了」)。**Routes 加成功 ≠ forEach 完成**。

#### 10.2.3 HMR ws 連 container internal IP(entry 004)

webpack-dev-server 4 預設 `client.webSocketURL = 'auto://[bind-host]:[bind-port]/ws'`。container 內 bind `0.0.0.0:8080`,publicHost detect 出 `172.19.0.2`(docker bridge IP)。host 上的 browser 沒 route → `ERR_CONNECTION_TIMED_OUT`。

修法 `auto://0.0.0.0:0/ws` 讓 client 用 page 的 `location.host`/`location.port`(就是 `127.0.0.1:8080`)— 這條路徑 user 已經走通,經 docker port-forward 進 container。

> 配 `CHOKIDAR_USEPOLLING=1` + `WATCHPACK_POLLING=true`(在 docker-compose.yml 已加)用:polling 偵測 host 改檔,WS 把 reload signal 推給 browser。**兩者缺一不可**。

#### 10.2.4 element-plus el-menu 觸發紅色 overlay(entry 005)

webpack-dev-server v4 把 runtime error 用 iframe overlay 全屏蓋頁(`#webpack-dev-server-client-overlay`),由 `client.overlay.errors: true` 觸發。

element-plus 2.13.7 / 2.14.0 都有 bug:
```js
Array.from(menu.value.childNodes).filter(item =>
  item.nodeName !== "#comment" && (item.nodeName !== "#text" || item.nodeValue))
```
保留 nodeValue 非空的 text node → 傳給 `getComputedStyle` → throw(該函式只吃 Element)。Menu 本身 render 正常,但 overlay 蓋整頁。

**升級 element-plus 不修這個**(已驗 2.14.0 = latest 還是同 buggy filter)。`runtimeErrors: false` 把 runtime overlay 關掉(compile error overlay 仍保留)。

### 10.3 額外 lessons learned

| 教訓 | 改變 |
|---|---|
| 看到「明明改了卻沒效果」第一念頭是 cache | 改完檔 → bundle grep 確認 → 沒進來再 `rm -rf node_modules/.cache` + restart |
| router.afterEach 的 from/to 是已 normalize 後的 location | NavigationFailureDuplicated 時 from===to 都是當前位置;真正 push 的 target 要從 `_push` hook arg 拿 |
| `console.log` 含物件,CDP 拿到的是 RemoteObject preview | `Runtime.consoleAPICalled` 的 args 用 `a.value || a.preview && JSON.stringify(a.preview) || a.description` 才能展開 |
| 紅色遮罩 ≠ JS bug | webpack-dev-server 自己加的 dev-only overlay。看到先確認 `#webpack-dev-server-client-overlay` 是不是 host element,再決定是否把它關掉 |
| 改 sub-repo 多個小 bug 時,patch log 用 NNN 編號獨立 entry | 不要把 5 個改動塞 1 個 entry — 每個 entry 都有自己的 Trigger / Detection,reconciliation 才能逐項驗對 |

### 10.4 速查表(本次新增)

```bash
# (A) 看當前 page WS URL(restart container 後 page ID 不變,因 browser 沒重啟)
export PAGE_WS=$(curl -s http://127.0.0.1:9229/json | python3 -c "
import sys, json
ps = json.load(sys.stdin)
print([p['webSocketDebuggerUrl']
       for p in ps
       if p.get('type')=='page' and 'go-admin' in p.get('title','')][0])")

# (B) Bundle freshness check
curl -s http://127.0.0.1:8080/js/app.js | grep -c "你剛加的標識字串"

# (C) 清 webpack persistent cache + restart(改檔沒生效時的下一步)
docker exec go-admin-ui rm -rf /app/node_modules/.cache
docker compose --profile sqlite restart go-admin-ui

# (D) HMR ws 確認(看到 created + handshake 101 才算通)
# 在 CDP node script 訂閱:
#   Network.webSocketCreated  → expect ws://127.0.0.1:8080/ws (而非 172.19.0.2)
#   Network.webSocketHandshakeResponseReceived  → expect status 101
```

---

## 11. 第三輪排查:外部資源稽核 + 視覺破壞(2026-05-09 終局)

第二輪後,功能上 login → dashboard 鏈路通了,但**視覺/隱私**還有兩塊:

1. 全程到底還在抓哪些**外部資源**?(隱私 / 離線 / 破圖)
2. dashboard 的 sidebar 文字**看不見**(白字白底 — visual regression)

每一塊都暴露新的 CDP 技法 + 跨 sub-repo 修法 + 一個「以為修好了實則沒套用」的 second-order bug(Vue CLI 5 + sass-loader 13 讓 scss `:export` 變成空 object)。

### 11.0 TL;DR

| 症狀 | 根因 | 修法(已記入 patch logs)|
|---|---|---|
| login → dashboard 全程載 3 個外部 host(`hm.baidu.com` / `doc-image.zhangwj.com` / `wpimg.wallstcn.com`)| 模板殘留:`App.vue` 內嵌 Baidu Analytics injection;`db.sql` seed 把 logo URL 指向作者私有 CDN;`sys_user.go` 預設 avatar 寫死 vue-element-admin 的 demo CDN | UI entry 006:刪 `App.vue` mounted body;backend entry 002:`db.sql` logo 改 `''` + 對 `<workspace>/config/go-admin-db.db` sqlite UPDATE;backend entry 003:`sys_user.go` 預設 avatar 改 `''` |
| sidebar 「go-admin管理系統」以下完全黑色,文字看不見 | `<el-menu>` 的 `:background-color` / `:text-color` / `:active-text-color` props 在 Element Plus 2.x 已 deprecated,實際被忽略 → `.el-menu` 套 EP 預設(白底);items color 透過其他規則設成 white → 白字白底 | UI entry 007:`Sidebar/index.vue` 改用 `:style` 設 `--el-menu-bg-color` 等 CSS variables |
| 修了 entry 007 後 inline style 仍只有 1/4 CSS var 套上 | `import variables from '@/styles/variables.scss'` 在 Vue CLI 5 + sass-loader 13+ 拿到的是 `{}` — `:export` 是 css-modules feature,需要 file 被當 module 處理 | UI entry 007 補完:不再依賴 `variables.menuBg` 等,直接 hardcode JS 字面值對齊 scss 的色票 |

### 11.1 新 CDP 技法

#### 11.1.1 全程 Network 請求收集 + hostname 過濾 + 分組

§10.1.3 介紹過 Network event 訂閱,但只看 `/api/v1/`。本輪要的是「**所有非 127.0.0.1 host 的請求**」。重點是用 `Network.requestWillBeSent` 收所有 request,然後在 JS 端用 `URL` parser filter:

```js
const reqs = new Map();   // requestId → { url, type, status, failed, errorText }

ws.on("message", m => {
  const msg = JSON.parse(m);
  // ... pending RPC dispatch ...
  if (msg.method === "Network.requestWillBeSent") {
    reqs.set(msg.params.requestId, {
      url: msg.params.request.url,
      type: msg.params.type,         // Image / Script / XHR / Stylesheet / WebSocket / Fetch / ...
    });
  }
  if (msg.method === "Network.responseReceived") {
    const r = reqs.get(msg.params.requestId);
    if (r) r.status = msg.params.response.status;
  }
  if (msg.method === "Network.loadingFailed") {
    const r = reqs.get(msg.params.requestId);
    if (r) { r.failed = true; r.errorText = msg.params.errorText; }
  }
});
```

跑完 user flow 後,filter + group by hostname:

```js
const externals = Array.from(reqs.values()).filter(r => {
  if (!r.url || r.url.startsWith("data:") || r.url.startsWith("blob:")) return false;
  try { return new URL(r.url).hostname !== "127.0.0.1"; }
  catch (e) { return false; }
});

// group by hostname for human readability
const grouped = {};
for (const r of externals) {
  const h = new URL(r.url).hostname;
  (grouped[h] = grouped[h] || []).push(r);
}
```

> 注意 `localhost` 跟 `127.0.0.1` 在字面上不同,但都是 loopback。第一輪稽核保留 `localhost`(後端 API),第二輪 post-fix 對照才把 `localhost` 也排除掉看「真外部」是否歸零。

#### 11.1.2 Computed style + CSS variable resolution

`getComputedStyle(el)` 拿 resolved style;若你想看「**某個 CSS custom property 解析後實際值**」,用 `.getPropertyValue('--xxx')`:

```js
const menu = document.querySelector(".sidebar-container .el-menu");
const style = getComputedStyle(menu);
const probe = {
  bg_resolved:  style.backgroundColor,                      // rgb()
  bg_var:       style.getPropertyValue("--el-menu-bg-color"), // 原始字串("#fff" / "#001529")
  inline_only:  menu.style.cssText,                          // <style="..."> 的字面 — 不含繼承來的
};
```

修 sidebar 那輪的關鍵發現:`var_bg: "#fff"` ← 表明 EP 預設值生效;設了 `--el-menu-bg-color: #001529` 之後立刻變 `rgb(0, 21, 41)`。

#### 11.1.3 Inline style vs runtime computed style 對比

Vue 在 `:style` 綁定時,**`undefined` / `null` / `''` 的值會被 silently 砍掉**,inline style 只剩 truthy 那幾條。這是「為什麼 setProperty 在 console 直接設有效,但 source 改了卻沒套上」的關鍵 debug 點:

```js
// 從 menu.style.cssText 看實際 inline 出現了什麼:
// 期望: "--el-menu-bg-color: #001529; --el-menu-hover-bg-color: #000c17; --el-menu-text-color: #fff; --el-menu-active-color: #409EFF;"
// 實際: "--el-menu-text-color: #fff;"   ← 其他 3 個沒進 inline,代表 computed 拿到 undefined
```

→ 此時往回追 computed property 的源頭(本輪是 scss `:export` 失效)。

#### 11.1.4 Vue 3 component instance 從 DOM 反查

從某個 DOM element 找它所屬的 Vue 3 component instance(看其 `proxy` / computed / data):

```js
function findInstance(el) {
  while (el) {
    if (el.__vueParentComponent) return el.__vueParentComponent;  // Vue 3 internal slot
    el = el.parentElement;
  }
  return null;
}

// 從 instance 走 .parent 鏈找指定名字的 component
let target = findInstance(document.querySelector(".sidebar-container"));
while (target) {
  const name = target.type?.__name || target.type?.name;
  if (name === "Sidebar") break;
  target = target.parent;
}
const proxy = target?.proxy;
console.log({
  themeStyle: proxy?.$store?.state?.settings?.themeStyle,
  variables: proxy?.variables,           // ← 這次發現是 {}
  menuCssVars: proxy?.menuCssVars,
});
```

> 失敗 fallback:某些 build / Vue config 下 `__vueParentComponent` 不在 DOM 上(treeshake / 不同 runtime build)。本輪實際取不到 component instance,改走「在 source 加 `console.log` 印 computed value」的 11.1.5 招(已記 §10.1.6)。

### 11.2 視覺 / 隱私 bugs 解剖

#### 11.2.1 外部資源稽核(三條)

完整流程:CDP 訂閱 Network → clearBrowserCookies → navigate `/` → 等 login render → 模擬填表單 → click 登入 → 等 dashboard render → dump `reqs` Map。

CDP 抓到 22 條 request 中,15 條是 host ≠ `127.0.0.1`。剔除 `localhost`(後端 API 5 條,屬同機 docker stack 內部)後,**真外部 7 條 / 4 unique URL / 3 host**:

| Host | Type | 來源 |
|---|---|---|
| `hm.baidu.com` | Script + Image × 2 | `App.vue` `mounted()` 內嵌 hm.js loader,page load 立刻 inject |
| `doc-image.zhangwj.com` | Image (cert expired → ERR_CERT_DATE_INVALID) | `db.sql` line 128 `sys_app_logo` seed value;backend `/api/v1/app-config` 回給 frontend `<img :src="sysInfo.sys_app_logo">` |
| `wpimg.wallstcn.com` | Image | `sys_user.go:449` `mp["avatar"] = "https://wpimg..."`;backend `/api/v1/getinfo` 回給 frontend → vuex `user/SET_AVATAR` → navbar `<img :src="user.avatar">` |

修法分三條 entry(UI 006、backend 002+003),**修完二次 CDP 重掃 → 真外部 0 條**(20260509_cdp9229_external-resources-audit.md 完整紀錄)。

#### 11.2.2 sidebar 白底白字(Element Plus 2.x 棄用 props)

screenshot:`go-admin管理系統` header 之下整片黑色,看不到任何 menu item。CDP 探:

```
.el-menu          { background-color: rgb(255, 255, 255), color: rgb(0, 0, 0) }   ← EP 預設白底黑字
.el-menu-item     { color: rgb(255, 255, 255), background: transparent }          ← 白字
```

**白底 + 白字 = 隱形**。但 source `Sidebar/index.vue` 明明有設 `:background-color="..."`:

```vue
<el-menu :background-color="themeStyle === 'light' ? variables.menuLightBg : variables.menuBg"
         :text-color="themeStyle === 'light' ? 'rgba(0,0,0,.65)' : '#fff'"
         :active-text-color="$store.state.settings.theme">
```

這三個 props 在 **Element Plus 2.x 已 deprecated**,silently 被忽略,而 EP 改用 CSS variables 讀色:

| Element Plus 2.x CSS variable | 用途 |
|---|---|
| `--el-menu-bg-color` | 背景色 |
| `--el-menu-text-color` | 文字色 |
| `--el-menu-active-color` | active item 色 |
| `--el-menu-hover-bg-color` | hover 背景色 |

**修法**:把 props 換成 inline `:style="menuCssVars"`,在 computed 算出對應 css var dictionary。

#### 11.2.3 scss `:export` 在 Vue CLI 5 失效

修了 entry 007 第一版後,inline style 只出現 `--el-menu-text-color: #fff`(literal 的那一條),其他 3 個用 `variables.menuBg` 等的全部 missing。

source 加 `console.log(variables)` 收集回來 → **`variables = {}`**。

`@/styles/variables.scss` 結尾的 `:export { menuBg: $menuBg; ... }` 是 css-modules 的 feature,需要 file 被當作 CSS Module 處理。Vue CLI 5 + sass-loader 13+ 對 non-`.module.scss` 檔不啟用 css-modules,`:export` block 變成普通 CSS(被 sass parser 接受但不產生 JS export)。

**修法選項**:
- A) 把檔名改 `variables.module.scss` + 改所有 import — 動到所有 consumer,blast radius 大
- B) 配置 css-loader 對 `variables.scss` 啟用 modules — 動 webpack config,不在 vue-cli 標準 surface
- C) **直接 hardcode JS 字面值在 menuCssVars,跟 scss 各自為政** — 1 個 call site,選這條

採 C。在 `menuCssVars` computed 裡寫死十六進位色碼,加註解明寫「scss `:export` 在 Vue CLI 5 拿到空 object,跟 variables.scss 同步維護」。

> 替代方向:用 `<style lang="scss">` 在 sidebar component 內 `@import` variables 並寫 CSS rule,讓 scss 在 build time 解析。這對 element-plus 注入 style 不便宜(要寫 deep selector 蓋過 EP 的 default rule),反而比 inline `:style` 麻煩。

### 11.3 操作觀察

#### 11.3.1 WSL2 Docker Desktop 的 bind-mount restart 玻璃心

修 backend `sys_user.go` 後跑 `docker compose restart go-admin-sqlite` 噴:

```
Error response from daemon: error while creating mount source path
'/run/desktop/mnt/host/wsl/docker-desktop-bind-mounts/Ubuntu-24.04/<sha>':
mkdir: file exists
```

連 `stop + up` 也同樣噴。**用 `rm -f + up -d` 才能繞過**:

```bash
docker compose --profile sqlite rm -f go-admin-sqlite
docker compose --profile sqlite up -d go-admin-sqlite
```

`rm -f` 把 stopped container 完全 remove,`up -d` 重建一個 fresh 的,bind-mount path 重新分配。

#### 11.3.2 修 sqlite seed 的 dev 資料 — 直接 sqlite UPDATE

遇到「db.sql 是 seed,但運行中的 sqlite db 是 binary 檔,改 sql 對它無效」這種情境:

```bash
# inside backend container (sqlite3 binary already provided by alpine sqlite package)
docker exec go-admin-sqlite sqlite3 /go-admin-db.db \
  "UPDATE sys_config SET config_value='' WHERE config_key='sys_app_logo';"
```

這個 db 透過 `<workspace>/config/go-admin-db.db` bind-mount 進 container,屬 workspace-owned binary(per Rule 1,不在任何 sub-repo 內),所以不需要 patch entry。但要在相關 sub-repo entry 的 Reason / Recovery 區塊**附帶記下 workspace-level 也做了 UPDATE**,避免下次 user 換 db 時忘了同步。

#### 11.3.3 Detection grep 不要踩自己的 patch 註解

寫 patch entry 的 Detection 命令時,常用「外部 host 字串 absent 」當判斷:

```bash
! grep -q "wpimg.wallstcn.com" fork260506-go-admin/app/admin/apis/sys_user.go
```

但 patch 自身的「為什麼移除這個 URL」的 comment 也常引用 host name:

```go
// Default avatar emptied: original value pointed at a vue-element-admin
// demo CDN (wpimg.wallstcn.com), which leaked dev usage to a third party
mp["avatar"] = ""
```

→ grep `wpimg.wallstcn.com` 仍 match 到 comment,Detection 永遠回 1 → reconciliation 把 patched 狀態誤判為「未套用」。

修法:**grep 真實代碼簽章**(只在原始 buggy code 出現的字串),而非 host name。例如:
- `mp["avatar"] = "https://wpimg.wallstcn.com/`(完整 literal,patched 後沒了)
- 或更穩:用 buggy code 中的 unique UUID `f778738c-e4f8-4870-b634-56703b4acafe`

App.vue 同樣狀況:用 `_hmt`(JS global var,只在 inject 代碼出現)當 detection,而非 `hm.baidu.com`(會 match 到註解)。

### 11.4 速查表(本輪新增)

```bash
# (E) 全 Network 收集 + 非 127.0.0.1 過濾
# 在 node CDP script 內(關鍵 method 訂閱):
#   Network.requestWillBeSent  → 收 url/type
#   Network.responseReceived   → 套 status
#   Network.loadingFailed      → 套 errorText
# 跑完 flow 後 filter `new URL(r.url).hostname !== "127.0.0.1"`,group by hostname

# (F) Computed CSS variable 解析
# Runtime.evaluate:
#   getComputedStyle(el).getPropertyValue("--xxx-foo")
# 比對 inline 設了 vs 實際 EP 套到的:
#   el.style.cssText            ← 自己 inline 寫了什麼
#   getComputedStyle(el).fooBar ← 實際 render 後的值

# (G) 從 DOM element 反查 Vue 3 component instance
# Runtime.evaluate:
#   let el = document.querySelector(".target");
#   while (el && !el.__vueParentComponent) el = el.parentElement;
#   const inst = el?.__vueParentComponent;
#   inst?.proxy.someComputed   ← 直接呼叫 component 的 computed/data

# (H) WSL2 Docker Desktop bind-mount restart 失敗
docker compose --profile sqlite rm -f <service>
docker compose --profile sqlite up -d <service>

# (I) 修 sqlite-mode 運行中 dev seed
docker exec go-admin-sqlite sqlite3 /go-admin-db.db "UPDATE ... SET ... WHERE ...;"

# (J) 寫 patch entry Detection 時的反例
# Bad:  ! grep -q "wpimg.wallstcn.com" file   ← patch 自己的 comment 也 match
# Good: ! grep -q "f778738c-e4f8-4870-b634"   ← 只在 buggy code 出現的 unique 字串
```
