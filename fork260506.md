# fork260506 工作區會談記錄

> 日期:2026-05-06
> 範圍:`fork-go-admin/` 下 7 個 `fork260506-*` git repo 的分支狀態,以及 `fork260506-go-admin` 對 `go-admin-core` 的依賴版本探索與本地 `replace` 用法。

---

## 1. 七個 repo 的分支狀態總覽

| Repo | 當前分支 | 與遠端對齊 | 工作區乾淨 | HEAD commit |
|---|---|---|---|---|
| fork260506-go-admin | `master` | 同步 origin/master | 乾淨 | `a5cc0a9` Add read and write timeout to HTTP server |
| fork260506-go-admin-core | `master` | 同步 origin/master | 乾淨 | `8a92061` Merge PR #47 from go-admin-team/dev |
| fork260506-go-admin-doc | `master` | 同步 origin/master | 乾淨 | `424855a` install.md 圖片連結改 HTTPS |
| fork260506-go-admin-ui | `master` | 同步 origin/master | 乾淨 | `e158066` Update README.md |
| fork260506-gorm-adapter | `master` | 同步 origin/master | 乾淨 | `bc66b93` Update go.mod |
| fork260506-redis-watcher | `master` | 同步 origin/master | 乾淨 | `bfe327c` upgrade to 1.20 |
| fork260506-redisqueue | `master` | 同步 origin/master | 乾淨 | `508101c` upgrade to 1.20 |

**整體狀態:**
- 7 個 repo 全部都在 `master` 分支
- 全部都與各自的 `origin/master` 同步(沒有 ahead/behind)
- 全部工作區乾淨,沒有未提交的變動
- 全部 origin 都指向 `github.com/miso168net/fork260506-*`(個人 fork)

---

## 2. fork260506-go-admin 各分支對 go-admin-core 的依賴

### Q:除了 master,有沒有更新版本?

**沒有,master 是最新的。**

| 分支 | go-admin-core | sdk | 比較 |
|---|---|---|---|
| **master** | `v1.5.3-rc.3.0.20250408121721-2763de5dcdf4` | 同上 | 最新(2025-04-08) |
| origin/dev | `v1.3.8` | `v1.3.9` | 較舊 |
| origin/dev1.18 | `v1.3.8` | `v1.3.9` | 較舊 |
| origin/1.3.x | `v1.3.5-rc.1` | 同上 | 最舊 |
| origin/dependabot/* (4 個) | `v1.5.2-0.20231103105356-84418ed9252c` | 同上 | 比 master 舊(2023-11-03) |

> 順帶一提:master 的 go.mod 末尾原本有兩行被註解的 `replace`,把這兩個依賴指向本地的 `../go-admin-core`。註解狀態 → 沒有生效,目前還是用遠端的 module。

---

## 3. Go module pseudo-version 解析

### Q:`v1.5.3-rc.3.0.20250408121721-2763de5dcdf4` 後半段是怎麼來的?

這叫做 **Go module pseudo-version(虛擬版本)**,用在「想引用某個 commit,但那個 commit 沒有對應的 git tag」的情況。Go toolchain 自動產生這串字串。

格式拆解:

```
v1.5.3-rc.3 . 0.20250408121721 - 2763de5dcdf4
└─────┬────┘   └──┬──┘ └──┬──┘   └────┬─────┘
   基準 tag     固定0   UTC時間戳     commit hash
                       (YYYYMMDDHHMMSS)   (前 12 碼)
```

- **`v1.5.3-rc.3`** — 該 commit 之前最近的一個 git tag(基準版本)
- **`.0.`** — 中間固定填一個 `0`,代表「這是 `v1.5.3-rc.3` 之後、下一個正式 tag 之前的中間 commit」(語意化版本排序的小技巧,讓它排在 `v1.5.3-rc.3` 之後、`v1.5.3-rc.4` 之前)
- **`20250408121721`** — 該 commit 的作者時間,UTC,格式 `YYYYMMDDHHMMSS` → 2025-04-08 12:17:21 UTC
- **`2763de5dcdf4`** — 該 commit SHA 的前 12 個字元

這串完全由 Go 自動產生,不需要手寫。例如:

```
go get github.com/go-admin-team/go-admin-core@2763de5d
```

或 `go get …@master`,Go 會去 git 拉那個 commit,然後組出這個 pseudo-version 寫進 `go.mod`。

**為什麼要這樣設計?** Go modules 要能「按版本排序」(知道哪個比哪個新),光靠 commit hash 沒有順序性。把日期戳放進版本字串裡,字典序排序就等於時間排序,Go 的 minimum version selection (MVS) 才能運作。

> 一句話:**版本號前半是「最近的 tag 是誰」,後半是「這個 commit 是何時的、是哪個」。**

---

## 4. go.mod 末尾被註解的 replace

go.mod 末尾原本有:

```go
//replace (
//    github.com/go-admin-team/go-admin-core v1.5.2-0.20231103105356-84418ed9252c => ../go-admin-core
//    github.com/go-admin-team/go-admin-core/sdk v1.5.2-0.20231103105356-84418ed9252c => ../go-admin-core/sdk
//)
```

### Q:解除註解後,它會 overwrite 掉上面 require 的版本嗎?

**會 — 但這份檔案這樣直接解除註解不會生效**(版本對不上的陷阱)。

### `replace` 的本質

`replace` 不是「另外加一份依賴」,而是 **重新導向**:

> 「當有人 import `github.com/go-admin-team/go-admin-core` 時,**不要去 GitHub 拉 module**,改去這個位置(本地路徑或別的 module)。」

| 區塊 | 角色 |
|---|---|
| `require (...)` | 宣告「我用了這個 module,語意化版本是 X」 — 用來做版本選擇/依賴解析 |
| `replace (...)` | 宣告「實際抓檔案時去這裡拿,不要去原本的位置」 — 只影響來源 |

### 為什麼這個檔案直接解除註解不會生效

註解掉的 replace 左邊版本是 `v1.5.2-0.20231103105356-84418ed9252c`,
但 require 是 `v1.5.3-rc.3.0.20250408121721-2763de5dcdf4`。

Go 的 `replace` 規則:
- `module v1.5.2 => ../path` → **只**替換 `v1.5.2` 這個版本
- `module => ../path`(左邊不寫版本) → 替換**所有版本**

所以直接解除註解,Go 會:
1. 看 `require` 要 `v1.5.3-rc.3...`
2. 看 `replace` 只處理 `v1.5.2-...`
3. → 兩者沒交集 → 還是去 GitHub 抓 `v1.5.3-rc.3...`,本地路徑被忽略

### 為什麼 require + replace 同時存在很常見

典型開發場景:**同時開發兩個 repo**(monorepo-like)。

```
fork-go-admin/
├── fork260506-go-admin/         ← 主專案,go.mod 在這
└── fork260506-go-admin-core/    ← 被依賴的 module
```

平常 build:從 GitHub 抓 `go-admin-core` 的某個 tag/commit。
要本地改 `go-admin-core` 並立即測試:解開 `replace`,讓主專案直接讀 `../go-admin-core` 的原始碼,改完馬上 `go run` 就生效,不用 commit→push→`go get` 一輪。

開發完成後再把 `replace` 註解回去,push 出去的版本就回到「從 GitHub 抓正式版」。

---

## 5. fork260506-go-admin-core 的 multi-module 結構

`fork260506-go-admin-core` 這個 repo 是一個 **multi-module repository**,共有 4 個獨立 module:

| 路徑 | module name |
|---|---|
| `/go.mod` | `github.com/go-admin-team/go-admin-core` |
| `/sdk/go.mod` | `github.com/go-admin-team/go-admin-core/sdk` |
| `/plugins/logger/logrus/go.mod` | (plugins 子 module) |
| `/plugins/logger/zap/go.mod` | (plugins 子 module) |

每個 `go.mod` 各自宣告 module,所以它們在 Go 的世界裡是獨立發布的單位 — 這也解釋了為什麼 `fork260506-go-admin/go.mod` 必須**同時** require 這兩條:

```
github.com/go-admin-team/go-admin-core      v1.5.3-rc.3...
github.com/go-admin-team/go-admin-core/sdk  v1.5.3-rc.3...
```

它們不是 parent / child 關係,而是兩個平行的 module(只是恰好放在同一個 git repo 裡)。

---

## 6. 啟用本地 replace 的建議寫法

在 `fork260506-go-admin/go.mod` 末尾改成:

```go
replace (
	github.com/go-admin-team/go-admin-core     => ../fork260506-go-admin-core
	github.com/go-admin-team/go-admin-core/sdk => ../fork260506-go-admin-core/sdk
)
```

**重點說明:**

| 項目 | 說明 |
|---|---|
| 縮排 | 用 **Tab**(go.mod 慣例,`go mod tidy` 會自動轉成 tab) |
| 左邊**沒有版本號** | 代表「不管 require 寫什麼版本,一律走本地路徑」— 最不容易壞 |
| 路徑 `../fork260506-go-admin-core` | 相對於 `fork260506-go-admin/go.mod` 所在目錄 |
| 沒有 `// indirect` | replace 不需要 |

**路徑驗證**(從 `fork260506-go-admin/go.mod` 的位置往上一層,再進到 `fork260506-go-admin-core/`):

| replace 目標 | 對應實際路徑 | 該位置有 go.mod? |
|---|---|---|
| `../fork260506-go-admin-core` | `…/fork-go-admin/fork260506-go-admin-core/` | 有 |
| `../fork260506-go-admin-core/sdk` | `…/fork-go-admin/fork260506-go-admin-core/sdk/` | 有 |

兩個路徑都對得上各自的 `go.mod`,replace 會生效。

> 補充:`plugins/logger/zap` 也是獨立 module,目前是 indirect 依賴(`v1.5.2`)。如果本地修改也包含這個 plugin,要再加一條 replace 指向 `../fork260506-go-admin-core/plugins/logger/zap`。一般情況不用動。

**操作步驟(手動執行):**

1. 把檔案最末段那 4 行被註解的舊 `replace` 整段刪掉(那是過期的版本號,留著只會誤導)
2. 貼上上面這段新的 replace
3. 執行 `go mod tidy` 讓 Go 重新計算
4. 用 `go list -m github.com/go-admin-team/go-admin-core` 驗證 — 輸出若顯示 `=> ../fork260506-go-admin-core` 就成功

**不想啟用時要怎麼還原?** 直接把這 4 行刪掉(或註解掉,但建議刪掉避免下次又踩同樣的版本對不上陷阱)。`require` 區塊不用動,Go 又會回去 GitHub 抓正式版。

---

## 7. 各 repo 分支活躍度分析

> 基準日期:2026-05-06
> 圖標:🟢 最新活躍 / 🟡 次新 / ⚪ 已停滯

### 7.1 fork260506-go-admin

| 分支 | 最後 commit | 距今 | commit |
|---|---|---|---|
| 🟢 **master** | 2025-09-10 | 8 個月前 | `a5cc0a9` HTTP server timeout |
| 🟡 origin/dependabot/protobuf-1.33.0 | 2024-09-03 | 1 年 8 個月前 | dependabot |
| 🟡 origin/dependabot/x/net-0.23.0 | 2024-09-03 | 1 年 8 個月前 | dependabot |
| ⚪ origin/dependabot/pgx-5.5.4 | 2024-07-05 | 1 年 10 個月前 | dependabot |
| ⚪ origin/dependabot/x/image-0.18.0 | 2024-06-26 | 1 年 10 個月前 | dependabot |
| ⚪ origin/dev | 2022-12-30 | 3 年 4 個月前 | 古老 PR merge |
| ⚪ origin/dev1.18 | 2022-08-08 | 3 年 9 個月前 | merge master |
| ⚪ origin/1.3.x | 2021-06-01 | 4 年 11 個月前 | docs |

→ **master 最新**,dev 系列都是 3+ 年前的歷史分支。

### 7.2 fork260506-go-admin-core ⚠️

| 分支 | 最後 commit | 距今 | commit |
|---|---|---|---|
| 🟢 **origin/dev** | **2026-04-19** | **2 週前** | `e993a05` feat(logger): PII mask hook |
| 🟡 origin/dependabot/x/net-0.23.0 | 2024-04-19 | 2 年 1 個月前 | dependabot |
| 🟡 origin/dependabot/sdk/pgproto3-2.3.3 | 2024-03-14 | 2 年 2 個月前 | dependabot |
| 🟡 origin/dependabot/sdk/protobuf-1.33.0 | 2024-03-13 | 2 年 2 個月前 | dependabot |
| 🟡 origin/dependabot/protobuf-1.33.0 | 2024-03-13 | 2 年 2 個月前 | dependabot |
| 🟡 origin/dependabot/sdk/pgx-4.18.2 | 2024-03-12 | 2 年 2 個月前 | dependabot |
| ⚪ origin/up | 2024-01-08 | 2 年 4 個月前 | refactor |
| ⚪ origin/dependabot/sdk/x/crypto-0.17.0 | 2023-12-18 | 2 年 5 個月前 | dependabot |
| ⚪ origin/dependabot/sdk/base64Captcha-1.3.6 | 2023-12-12 | 2 年 5 個月前 | dependabot |
| ⚪ **master** | **2022-11-24** | **3 年 5 個月前** | `8a92061` |
| ⚪ origin/v1.4.0 | 2022-08-09 | 3 年 9 個月前 | merge |
| ⚪ origin/temp | 2022-08-09 | 3 年 9 個月前 | casbin redis watcher |

→ master 停在 3 年 5 個月前,但 **dev 持續活躍**,2 週前才有更新(PII mask hook + caller skip)。
→ 這個 repo 真正的「最新版」在 `dev` 分支,master 已被冷落。
→ 這也解釋了 `fork260506-go-admin/master/go.mod` 為何用 pseudo-version 鎖到一個 commit hash:dev 上的最新內容沒有被合進 master 也沒打 tag,只能用 `v1.5.3-rc.3.0.YYYYMMDD-hash` 直接指向某個 dev 分支的 commit。

### 7.3 fork260506-go-admin-doc

| 分支 | 最後 commit | 距今 | commit |
|---|---|---|---|
| 🟢 **master** | 2025-04-09 | 1 年 1 個月前 | install.md HTTPS |
| ⚪ origin/master-old | 2022-06-11 | 3 年 11 個月前 | merge |
| ⚪ origin/main | 2022-05-31 | 3 年 11 個月前 | docs |
| ⚪ origin/gh-pages | 2021-08-20 | 4 年 9 個月前 | deploy |
| ⚪ origin/1.3.x | 2021-06-30 | 4 年 10 個月前 | update |
| ⚪ origin/1.3.2 | 2021-05-29 | 4 年 11 個月前 | update |

→ master 最新,其他都是歷史。

### 7.4 fork260506-go-admin-ui ⚠️

| 分支 | 最後 commit | 距今 | commit |
|---|---|---|---|
| 🟢 **origin/dev-to-vue3** | **2026-01-31** | **3 個月前** | `3a96323` chart-card 插槽語法 |
| 🟡 origin/vue3_dev | 2025-06-25 | 11 個月前 | 字典資料修復 |
| ⚪ **master** | **2024-09-05** | **1 年 8 個月前** | Update README.md |
| ⚪ dependabot 系列 | 2023-11 | 2 年 6 個月前 | npm 套件 |
| ⚪ origin/github-dev | 2022-11-01 | 3 年 6 個月前 | refactor ci |
| ⚪ origin/vue3_disuse | 2022-09-14 | 3 年 8 個月前 | merge |
| ⚪ origin/dev | 2022-02-13 | 4 年 3 個月前 | version bump |
| ⚪ origin/1.3.x | 2021-06-03 | 4 年 11 個月前 | bug fix |
| ⚪ origin/v1 | 2020-07-09 | 6 年前 | merge |
| ⚪ origin/add-license-1 | 2020-03-14 | 6 年前 | LICENSE |

→ master 也是停滯,真正活躍的是 `dev-to-vue3`(3 個月前),`vue3_dev` 也較新(11 個月前)。
→ 暗示專案主軸在從 Vue2 轉 Vue3。

### 7.5 fork260506-gorm-adapter

| 分支 | 最後 commit | 距今 | commit |
|---|---|---|---|
| 🟡 **master** | 2022-11-27 | 3 年 5 個月前 | Update go.mod |
| ⚪ origin/dev | 2022-11-04 | 3 年 6 個月前 | feat: filtered adapter |

→ **整個 repo 已停滯**,master 仍是最新。
→ 注意:這是個人 fork,**上游(casbin/gorm-adapter)其實還在更新**,只是這份 fork 沒同步而已。

### 7.6 fork260506-redis-watcher

| 分支 | 最後 commit | 距今 | commit |
|---|---|---|---|
| 🟡 **master** | 2023-11-02 | 2 年 6 個月前 | upgrade to 1.20 |

→ 只有一條 master,2 年半前停。

### 7.7 fork260506-redisqueue

| 分支 | 最後 commit | 距今 | commit |
|---|---|---|---|
| 🟡 **master** | 2023-11-02 | 2 年 6 個月前 | upgrade to 1.20 |

→ 同上,只有一條 master。

---

## 8. 重點觀察彙整

| Repo | master 最後 commit | 最活躍分支 | 是否 master 已被超越? |
|---|---|---|---|
| fork260506-go-admin | 2025-09-10(8 個月前) | master | ❌ master 最新 |
| **fork260506-go-admin-core** | **2022-11-24(3.5 年前)** | **dev(2 週前)** | ⚠️ **是,差距 3 年 5 個月** |
| fork260506-go-admin-doc | 2025-04-09(1 年 1 月前) | master | ❌ master 最新 |
| **fork260506-go-admin-ui** | **2024-09-05(1 年 8 月前)** | **dev-to-vue3(3 個月前)** | ⚠️ **是,差距 1 年 5 個月** |
| fork260506-gorm-adapter | 2022-11-27(3.5 年前) | master | ❌ 但整個 repo 都停滯 |
| fork260506-redis-watcher | 2023-11-02(2.5 年前) | master | ❌ 但整個 repo 都停滯 |
| fork260506-redisqueue | 2023-11-02(2.5 年前) | master | ❌ 但整個 repo 都停滯 |

**兩個 repo 的 master 已被「真正的開發分支」明顯超越:**

1. **go-admin-core**:dev 比 master 新 3 年 5 個月 — 這就是為什麼 `fork260506-go-admin/master/go.mod` 必須用 pseudo-version 從 dev 抓 commit。
2. **go-admin-ui**:dev-to-vue3 比 master 新 1 年 5 個月 — 主版本在做 Vue2 → Vue3 遷移。

如果要追上游最新,**這兩個 repo 不能只看 master**,要分別追 `dev` 與 `dev-to-vue3`。

---

## 9. 統一改用 main 為 default branch(2026-05-06 操作紀錄)

### 9.1 操作目標

把 7 個 repo 的 GitHub default branch 從 `master` 改成 `main`,以對齊現代命名慣例。並依各 repo 真正活躍的分支決定 `main` 的內容來源。

### 9.2 兩種情境的命令模板

**情境 A:master 即活躍分支(5 個 repo)**

```bash
git checkout master
git pull
git branch main master       # 本地建 main 指向 master 的 HEAD
git push -u origin main      # 推到遠端(自動 fast-forward 或新建)
gh repo edit --default-branch main
git checkout main
```

**情境 B:活躍分支不是 master(2 個 repo,以 go-admin-core 為例)**

```bash
git fetch
git checkout dev
git pull
git branch main dev
git push -u origin main
gh repo edit --default-branch main
git checkout main
```

### 9.3 7 個 repo 的處理結果

| # | Repo | main 來源 | main HEAD(對應 commit) | 備註 |
|---|---|---|---|---|
| 1 | fork260506-go-admin | master | `a5cc0a9` | 新建 main(原無此分支) |
| 2 | fork260506-go-admin-core | **dev** | `e993a05` | main 比 master 新 3 年 5 個月 |
| 3 | fork260506-go-admin-doc | master | `424855a` | 遠端原本已有 2022 年的舊 main(`fca2b47`),被 fast-forward 推進 |
| 4 | fork260506-go-admin-ui | **dev-to-vue3** | `3a96323` | main 比 master 新 1 年 5 個月 |
| 5 | fork260506-gorm-adapter | master | `bc66b93` | 新建 main |
| 6 | fork260506-redis-watcher | master | `bfe327c` | 新建 main |
| 7 | fork260506-redisqueue | master | `508101c` | 新建 main |

### 9.4 fast-forward 補充說明(以 go-admin-doc 為例)

`go-admin-doc` 在 GitHub 上早就存在一條 main 分支(2022 年的歷史遺留,HEAD `fca2b47`)。push 時 git 自動判斷:

```
fca2b47 ── (中間 N 個 commit) ── 424855a
   ↑                                ↑
舊遠端 main                  我要推上去的 master 內容
```

舊 commit 是新 commit 的祖先 → 可 fast-forward → 直接推進指標,不需要 merge,沒有歷史改寫:

```
   fca2b47..424855a  main -> main
```

如果不是 fast-forward(兩條線分歧),push 會被拒絕並要求 `git pull --rebase` 或 `--force`。

### 9.5 main 都建好後,新增 fork 元資訊檔案

每個 repo 的 main 分支根目錄新增了 `x_fork.branch-origin.md`,記錄:

- 此 repo / 推測的原始上游
- Fork 用途、Fork 建立日(2026-05-06)
- main 是從哪條分支建出來的(以及該分支當時的 HEAD)
- 為何要改用 main(不同 repo 理由不同)
- 比對 main 與 master 差異的指令範例

每個 repo 對應的 commit:

| Repo | commit hash | main push 範圍 |
|---|---|---|
| fork260506-go-admin | `8ad6a0d` | `a5cc0a9..8ad6a0d` |
| fork260506-go-admin-core | `7203d71` | `e993a05..7203d71` |
| fork260506-go-admin-doc | `545e092` | `424855a..545e092` |
| fork260506-go-admin-ui | `7fe9e0f` | `3a96323..7fe9e0f` |
| fork260506-gorm-adapter | `618f1d7` | `bc66b93..618f1d7` |
| fork260506-redis-watcher | `1f63773` | `bfe327c..1f63773` |
| fork260506-redisqueue | `182c960` | `508101c..182c960` |

### 9.6 git identity 設定方式(local,非 global)

第一次 commit 時失敗:

```
fatal: empty ident name (for <anew@ANEW-W11E24H2-5.localdomain>) not allowed
```

— 因為這台機器沒設過 git `user.name` / `user.email`。為避免影響全機所有 repo,選擇**只在這 7 個 repo 內**設定 local config:

```bash
git config user.name  "miso168net"     # 不加 --global
git config user.email "mis@o168.net"
```

這份 config 只寫進每個 repo 的 `.git/config`,不影響 `~/.gitconfig` 與其他專案。三種選項當時的取捨:

| 方法 | 範圍 | 副作用 |
|---|---|---|
| A. `--global` | 全機所有 repo | 一次設好,但 WSL 上 git 從此都用此身分 |
| **B. local(本次採用)** | 只影響這 7 個 repo | 之後在這些 repo commit 都用此身分;不影響其他專案 |
| C. inline `git -c user.name=...` | 只用於單次 commit | config 完全不動,但每次 commit 都得自己帶 |

### 9.7 最終驗證

```
Repo                            | Local HEAD   | Remote default
fork260506-go-admin             | main         | main
fork260506-go-admin-core        | main         | main
fork260506-go-admin-doc         | main         | main
fork260506-go-admin-ui          | main         | main
fork260506-gorm-adapter         | main         | main
fork260506-redis-watcher        | main         | main
fork260506-redisqueue           | main         | main
```

7 個 repo 的本地 HEAD 與 GitHub default branch 全部統一為 `main`;舊的 `master`、`dev`、`dev-to-vue3` 分支都保留未刪。

### 9.8 補充驗證:gh 直接查 GitHub 端 default branch

事後再用 `gh repo view --json defaultBranchRef` 直接打 GitHub API 確認(避免單純依賴本地快取):

```bash
for repo in fork260506-go-admin fork260506-go-admin-core fork260506-go-admin-doc \
            fork260506-go-admin-ui fork260506-gorm-adapter fork260506-redis-watcher \
            fork260506-redisqueue; do
  gh repo view "miso168net/$repo" --json defaultBranchRef -q .defaultBranchRef.name
done
```

結果:

| Repo | GitHub default branch |
|---|---|
| fork260506-go-admin | main ✓ |
| fork260506-go-admin-core | main ✓ |
| fork260506-go-admin-doc | main ✓ |
| fork260506-go-admin-ui | main ✓ |
| fork260506-gorm-adapter | main ✓ |
| fork260506-redis-watcher | main ✓ |
| fork260506-redisqueue | main ✓ |

7 個 repo 在 GitHub 端的 default branch 確認全部為 `main`,操作完整收尾。
